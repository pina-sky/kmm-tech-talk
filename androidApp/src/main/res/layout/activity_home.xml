<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="16dp"
    android:theme="@style/Theme.MaterialComponents.Light">

    <TextView
        android:id="@+id/txt_home_header"
        style="@style/Header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Home"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_rocket_launches"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:text="Rocket Launches (Suspend Example)"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/txt_home_header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_platform_version"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:text="Platform Version (Expect &amp; Actual Example)"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btn_rocket_launches" />

</androidx.constraintlayout.widget.ConstraintLayout>